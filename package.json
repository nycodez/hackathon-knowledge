{
  "name": "hackathon-knowledge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:api": "pnpm --filter @hackathon/api dev",
    "dev:web": "pnpm --filter @hackathon/web start",
    "setup:local": "bash scripts/setup-local.sh",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "build": "pnpm --filter @hackathon/web build && pnpm --filter @hackathon/api build",
    "typecheck": "pnpm -r typecheck",
    "db:migrate": "pnpm --filter @hackathon/api migrate",
    "db:seed": "pnpm --filter @hackathon/api seed",
    "verify:knowledge": "pnpm --filter @hackathon/api verify:chunks && pnpm --filter @hackathon/api verify:retrieval && pnpm --filter @hackathon/api verify:http"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "marked": "^18.0.6",
    "zone.js": "^0.15.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0"
  }
}
